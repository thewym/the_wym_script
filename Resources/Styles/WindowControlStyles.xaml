<!--  WindowControlStyles.xaml  -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--  窗口控制按钮样式（你的代码）  -->
    <Style x:Key="WindowControlButtonStyle" TargetType="Button">
        <!--  注意：这里的颜色值需要替换为主题画刷引用，见下方说明  -->
        <Setter Property="Foreground" Value="{StaticResource Button.Text.Brush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!--  引用主题画刷：按钮hover背景  -->
                            <Setter TargetName="border" Property="Background" Value="{StaticResource Button.Hover.Background.Brush}" />
                        </Trigger>
                        <Trigger Property="Tag" Value="Close">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <!--  引用主题画刷：关闭按钮hover背景  -->
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="{Binding Source={StaticResource Button.Close.Hover.Background.Brush}, Path=Color}"
                                            Duration="0:0:0.1" />
                                        <ColorAnimation
                                            Storyboard.TargetProperty="Foreground.Color"
                                            To="White"
                                            Duration="0:0:0.1" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="Transparent"
                                            Duration="0:0:0.1" />
                                        <ColorAnimation
                                            Storyboard.TargetProperty="Foreground.Color"
                                            To="{Binding Source={StaticResource Button.Text.Brush}, Path=Color}"
                                            Duration="0:0:0.1" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style
        x:Key="BtnBaseStyle"
        BasedOn="{StaticResource CommonButtonBase}"
        TargetType="{x:Type Button}">
        <!--  笔刷颜色  -->
        <Setter Property="Foreground" Value="{DynamicResource Btn.Normal.Foreground}" />
        <!--  背景颜色  -->
        <Setter Property="Background" Value="{DynamicResource Btn.Normal.Background}" />
        <!--  边框颜色  -->
        <Setter Property="BorderBrush" Value="{DynamicResource Btn.Normal.BorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  关闭按钮样式（核心修改：添加 X 矢量图）  -->
    <Style
        x:Key="BtnWinCloseStyle"
        BasedOn="{StaticResource BtnBaseStyle}"
        TargetType="{x:Type Button}">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Content">
            <Setter.Value>
                <Path
                    Width="12"
                    Height="12"
                    Data="M1,1 L11,11 M11,1 L1,11"
                    RenderOptions.EdgeMode="Aliased"
                    Stretch="None"
                    Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                    StrokeLineJoin="Miter"
                    StrokeThickness="1" />
            </Setter.Value>
        </Setter>

        <!--  原有交互触发器（保持不变，确保悬浮/按压效果正常）  -->
        <Style.Triggers>
            <!--  鼠标悬浮：背景变红、前景变白  -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Btn.WinClose.MouseOver.Background}" />
                <Setter Property="Foreground" Value="{DynamicResource Btn.WinClose.MouseOver.Foreground}" />
            </Trigger>
            <!--  鼠标按压：背景变浅红  -->
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Btn.WinClose.Pressed.Background}" />
                <Setter Property="Foreground" Value="{DynamicResource Btn.WinClose.Pressed.Foreground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
